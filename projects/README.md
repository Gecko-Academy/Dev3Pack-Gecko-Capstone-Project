# The daily projects

One lands here per day of week 3. Each is self-contained: its own folder, its own
README saying what you are building and how to tell it works, and its own check.

Pull them with `git pull upstream main`.

They build on each other, so doing them in order is easier. None is marked, and skipping
one does not lock you out of the next: each says what it assumes.
